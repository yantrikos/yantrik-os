// Re-export of the UI kit's y_input.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/y_input.slint — edit that file, not this one.)
export { YInput } from "../../../yantrik-ui-kit/slint/y_input.slint";
