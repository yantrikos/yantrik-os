// Re-export of the UI kit's y_tabs.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/y_tabs.slint — edit that file, not this one.)
export { YTabItem, YTabs } from "../../../yantrik-ui-kit/slint/y_tabs.slint";
