// Re-export of the UI kit's y_button.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/y_button.slint — edit that file, not this one.)
export { YButton, YIconButton } from "../../../yantrik-ui-kit/slint/y_button.slint";
