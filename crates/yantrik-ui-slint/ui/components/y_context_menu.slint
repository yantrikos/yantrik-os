// Re-export of the UI kit's y_context_menu.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/y_context_menu.slint — edit that file, not this one.)
export { YMenuAction, YContextMenu } from "../../../yantrik-ui-kit/slint/y_context_menu.slint";
