// Re-export of the UI kit's y_toolbar.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/y_toolbar.slint — edit that file, not this one.)
export { YToolbar } from "../../../yantrik-ui-kit/slint/y_toolbar.slint";
