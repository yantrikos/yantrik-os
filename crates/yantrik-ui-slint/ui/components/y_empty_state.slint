// Re-export of the UI kit's y_empty_state.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/y_empty_state.slint — edit that file, not this one.)
export { YEmptyState } from "../../../yantrik-ui-kit/slint/y_empty_state.slint";
