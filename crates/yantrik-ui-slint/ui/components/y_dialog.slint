// Re-export of the UI kit's y_dialog.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/y_dialog.slint — edit that file, not this one.)
export { YDialog } from "../../../yantrik-ui-kit/slint/y_dialog.slint";
