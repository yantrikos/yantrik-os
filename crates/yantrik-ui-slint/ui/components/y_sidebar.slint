// Re-export of the UI kit's y_sidebar.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/y_sidebar.slint — edit that file, not this one.)
export { YSidebarItem, YSidebarSection, YSidebar } from "../../../yantrik-ui-kit/slint/y_sidebar.slint";
