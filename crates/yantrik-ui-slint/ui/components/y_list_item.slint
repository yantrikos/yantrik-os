// Re-export of the UI kit's y_list_item.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/y_list_item.slint — edit that file, not this one.)
export { YListItem } from "../../../yantrik-ui-kit/slint/y_list_item.slint";
