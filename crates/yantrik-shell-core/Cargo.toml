[package]
name = "yantrik-shell-core"
version = "0.1.0"
edition = "2021"
description = "Yantrik OS shell core — app registry, screen routing, .desktop scanning"

[dependencies]
tracing = { workspace = true }

[target.'cfg(unix)'.dependencies]
# For PR_SET_PDEATHSIG: services must not outlive the shell that started them.
libc = { workspace = true }
