[package]
name = "system-monitor-service"
version = "0.1.0"
edition = "2021"
description = "Yantrik system monitor service — CPU, memory, disk, network, processes via /proc"

[[bin]]
name = "system-monitor-service"
path = "src/main.rs"

[dependencies]
yantrik-ipc-contracts = { workspace = true }
yantrik-service-sdk = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
libc = { workspace = true }
